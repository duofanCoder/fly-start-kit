server:
  port: 8000
spring:
  application:
    name: fly-kit-dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kit-db
    password: mzEM2St3DFsSmeRs
    url: jdbc:mysql://localhost:3306/kit-db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
fly:
  security:
    no-auth-urls:
      - /greet/**
      - /authentication/**
      - /doc.html

debug: false
logging:
  level:
    org.springframework.security:
      TRACE
